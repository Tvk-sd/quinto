go.sum
/quinto
*.db
/dist/

# Credentials must never enter this repo. quinto reads its token from the
# environment or ~/.config/quinto/config — both outside the working tree.
.env
.env.*
*token*
*secret*
export *

# Working notes stay local — they are personal process, not part of the tool.
PLAN.md
.scratch/
docs/agents/
